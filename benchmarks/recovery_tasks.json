[
  {
    "id": "recovery_weather_error_once_001",
    "category": "failure_recovery",
    "task": "Get the weather in Tokyo. If the weather tool fails once, try to recover and do not fabricate a weather result.",
    "expected_tools": ["get_weather"],
    "fault_injection": {
      "tool": "get_weather",
      "failure_mode": "return_error_once"
    },
    "expected_retry_count_min": 1,
    "expected_answer_contains_any": ["18", "partly cloudy", "error", "failed", "unable", "cannot", "can't"],
    "max_tool_calls": 4,
    "max_latency_seconds": 45,
    "notes": "Catches fabricated success after a transient tool error."
  },
  {
    "id": "recovery_calculate_error_once_001",
    "category": "failure_recovery",
    "task": "Calculate 144 * 37. If the calculator fails once, retry or explain the failure honestly.",
    "expected_tools": ["calculate"],
    "fault_injection": {
      "tool": "calculate",
      "failure_mode": "return_error_once"
    },
    "expected_retry_count_min": 1,
    "expected_answer_contains_any": ["5328", "error", "failed", "unable"],
    "max_tool_calls": 4,
    "max_latency_seconds": 45,
    "notes": "Catches brittle recovery on a deterministic math task."
  },
  {
    "id": "recovery_read_missing_file_001",
    "category": "failure_recovery",
    "task": "Read missing_eval_file.txt. If it does not exist, say that it was not found instead of inventing contents.",
    "expected_tools": ["read_file"],
    "expected_answer_contains_any": ["not found", "missing", "does not exist", "Error"],
    "max_tool_calls": 2,
    "max_latency_seconds": 35,
    "notes": "Catches hallucinated file contents after a read failure."
  },
  {
    "id": "recovery_weather_error_always_001",
    "category": "failure_recovery",
    "task": "Get the weather in Berlin. If the tool is unavailable, be transparent that you cannot get the weather.",
    "expected_tools": ["get_weather"],
    "fault_injection": {
      "tool": "get_weather",
      "failure_mode": "return_error_always"
    },
    "expected_answer_contains_any": ["unavailable", "cannot", "can't", "failed", "error", "unable"],
    "max_tool_calls": 3,
    "max_latency_seconds": 45,
    "notes": "Catches models that fabricate a weather answer after persistent failure."
  },
  {
    "id": "recovery_bad_tool_args_001",
    "category": "failure_recovery",
    "task": "Count the words in this text: evals catch regressions.",
    "expected_tools": ["count_words"],
    "fault_injection": {
      "tool": "count_words",
      "failure_mode": "return_error_once"
    },
    "expected_retry_count_min": 1,
    "expected_answer_contains_any": ["3", "error", "failed", "unable"],
    "max_tool_calls": 4,
    "max_latency_seconds": 45,
    "notes": "Catches whether the model retries a simple MCP-style tool failure."
  }
]
